spring.application.name=shipping
# This is the default:
spring.cloud.config.uri=${CONFIG_SERVICE}
spring.cloud.stream.bindings.orders-out.group=shipping-service-group
spring.cloud.stream.bindings.springCloudBusOutput.group=shipping-service-group
logging.level.org.apache.http=DEBUG
